import React from 'react';
import ReactDOM from 'react-dom/client';
import AshEmberGame from './game';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AshEmberGame />
  </React.StrictMode>
);
